[tool.poetry]
name = "fastapi"
version = "0.1.0"
description = ""
authors = ["guilherme"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
pydantic = "^2.5.3"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
